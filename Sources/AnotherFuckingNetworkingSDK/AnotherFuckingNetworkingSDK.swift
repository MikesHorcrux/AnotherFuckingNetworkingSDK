/// A type-safe, Swift-concurrency-ready networking library built on URLSession.
///
/// Create an ``APIClient`` with a base URL, define values conforming to
/// ``Request``, and call ``APIClient/send(_:)`` or ``APIClient/sendPage(_:)``.
